spring:
  application:
    name: step14

  datasource:
    # driver-class-name: org.h2.Driver
    # url: jdbc:h2:~/test
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:h2:~/test;AUTO_SERVER=TRUE;TIME ZONE=Asia/Seoul
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database: h2
    open-in-view: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show-sql: true
        format_sql: true
        use_sql_comments: false

  thymeleaf:
    prefix: file:src/main/resources/templates/
    cache: false

  data:
    web:
      pageable:
        default-page-size: 10
        one-indexed-parameters: true
  profiles:
    include: secret

jwt:
  secret-key: pAqbmyRv4Ec6ruN2ym5WUCKisQsEr41mcnoW4zSe3oC
  access-token-expiration: 10m
  refresh-token-expiration: 14d